% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsr_analyze.R
\name{tsr_behavior_profile}
\alias{tsr_behavior_profile}
\title{Aggregate behavioral profile for a side across many games}
\usage{
tsr_behavior_profile(games, side = 1L, label = NULL)
}
\arguments{
\item{games}{A list of \code{tsr_game} objects.}

\item{side}{Integer \code{1L} (X) or \code{2L} (O). The side to profile.}

\item{label}{Optional character. Label for the profile (e.g. a policy name).}
}
\value{
A one-row tibble with \code{label}, \code{side}, \code{n_games}, \code{n_moves_total},
\code{accuracy}, \code{mean_regret}, \code{blunder_rate}, \code{win_conversion},
\code{defense_rate}, \code{aggression}, \code{mean_decisiveness}.
}
\description{
Walks a list of \code{tsr_game} objects, analyses each, and computes the
aggregate profile for the chosen side (player X by default).
}
\examples{
\donttest{
g1 <- tsr_play_game("random", "random", seed = 1L)
g2 <- tsr_play_game("random", "random", seed = 2L)
tsr_behavior_profile(list(g1, g2), side = 1L, label = "random")
}
}
