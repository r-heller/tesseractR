% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsr_move.R
\name{tsr_move}
\alias{tsr_move}
\title{Make a move on a 4D tic-tac-toe board}
\usage{
tsr_move(board, i = NULL, j = NULL, k = NULL, l = NULL, cell = NULL)
}
\arguments{
\item{board}{A \code{ttt_board}.}

\item{i, j, k, l}{Integer coordinates in \code{0:3}. Either all four or none.}

\item{cell}{Optional linear cell index (\code{1:256}). Mutually exclusive with the
coordinate form.}
}
\value{
A new \code{ttt_board} with the mark placed, \code{to_move} flipped, and
\code{history} extended by the move.
}
\description{
Place the current player's mark at the given cell and return a \strong{new}
\code{ttt_board} with the move applied. The input board is never mutated.
}
\details{
Provide either the four hypercube coordinates \verb{(i, j, k, l)} (each in \code{0:3}),
or the linear cell index via \code{cell} (an integer in \code{1:256}). Exactly one form
must be supplied.
}
\examples{
b <- tsr_new_board()
b <- tsr_move(b, 0L, 0L, 0L, 0L)
b <- tsr_move(b, cell = 5L)
tsr_status(b)
}
