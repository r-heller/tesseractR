% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsr_plot.R
\name{tsr_plot_slice}
\alias{tsr_plot_slice}
\title{Plot a 3D slice of a 4D tic-tac-toe board}
\usage{
tsr_plot_slice(
  board,
  axis = c("i", "j", "k", "l"),
  at = 0L,
  highlight_win = TRUE,
  ...
)
}
\arguments{
\item{board}{A \code{ttt_board}.}

\item{axis}{One of \code{"i"}, \code{"j"}, \code{"k"}, \code{"l"}. The axis to hold fixed.}

\item{at}{Integer \code{0:3}. The value at which to fix \code{axis}.}

\item{highlight_win}{Logical. Emphasise the winning line if it lies in
(or intersects) the slice.}

\item{...}{Reserved for future arguments.}
}
\value{
A \code{ggplot} object.
}
\description{
Holds one of the four hypercube axes at a fixed value, dropping the board to
a 3D 4x4x4 cube, then renders that cube as a 1x4 strip of 4x4 mini-boards.
Use this view to make through-all-four-dimensions diagonals read as straight
lines within a single plane. Returns a \code{ggplot} object.
}
\details{
Convention: of the three remaining axes, the \strong{highest-numbered} is used as
the block axis (the strip dimension); the other two form each mini-board.
}
\examples{
tsr_plot_slice(tsr_new_board(), axis = "l", at = 0L)
}
