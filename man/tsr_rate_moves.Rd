% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsr_evaluate.R
\name{tsr_rate_moves}
\alias{tsr_rate_moves}
\title{Rate every legal move from a position}
\usage{
tsr_rate_moves(board, method = c("heuristic", "rollout"), n = 200L)
}
\arguments{
\item{board}{A \code{ttt_board}.}

\item{method}{One of \code{"heuristic"} (default) or \code{"rollout"}.}

\item{n}{Integer. Number of rollouts when \code{method = "rollout"}.}
}
\value{
A tibble with columns:
\code{cell} (integer linear index),
\code{i}, \code{j}, \code{k}, \code{l} (integer coordinates in \code{0:3}),
\code{score} (numeric raw evaluation after the move),
\code{win_prob} (numeric in \verb{[0, 1]}),
\code{rank} (integer; \code{1} = best),
\code{is_best} (logical),
\code{is_winning} (logical; the move completes a line),
\code{is_blocking} (logical; the move denies an opponent's immediate win).
Sorted by \code{rank}.
}
\description{
For each legal move, applies it and scores the resulting position from the
moving side's perspective. Returns a tibble sorted best-first with type-
stable columns; on a finished board returns a zero-row tibble with the same
schema.
}
\examples{
tsr_rate_moves(tsr_new_board())
}
