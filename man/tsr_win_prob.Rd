% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsr_evaluate.R
\name{tsr_win_prob}
\alias{tsr_win_prob}
\title{Calibrated win probability for a position}
\usage{
tsr_win_prob(
  board,
  player = NULL,
  method = c("heuristic", "rollout"),
  n = 200L
)
}
\arguments{
\item{board}{A \code{ttt_board}.}

\item{player}{Integer \code{1L} or \code{2L}, or \code{NULL} (default, uses \code{board$to_move}).}

\item{method}{One of \code{"heuristic"} (default) or \code{"rollout"}.}

\item{n}{Integer. Number of rollouts when \code{method = "rollout"}.}
}
\value{
Numeric scalar in \verb{[0, 1]}.
}
\description{
Returns the estimated probability in \verb{[0, 1]} that \code{player} wins from the
given position. Two methods are supported:
}
\details{
\itemize{
\item \code{"heuristic"} (default, real-time): maps \code{tsr_evaluate()} through a
logistic calibration \code{p = plogis((score - a) / b)}. The coefficients
\code{a}, \code{b} are fit by a \verb{data-raw/} script (S6) and stored as internal
package data; provisional defaults are used until calibration is run.
\item \code{"rollout"} (offline accuracy): runs \code{n} policy-guided Monte-Carlo
playouts using a light heuristic policy and returns the empirical win
share. Slower in pure R; intended for offline analysis, not the live UI.
}

Terminal positions short-circuit: a win returns \code{1}, a loss \code{0}, a draw \code{0.5}.
}
\examples{
tsr_win_prob(tsr_new_board())
\donttest{
tsr_win_prob(tsr_new_board(), method = "rollout", n = 20L)
}
}
