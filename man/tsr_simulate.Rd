% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsr_simulate.R
\name{tsr_simulate}
\alias{tsr_simulate}
\title{Simulate many games between two policies}
\usage{
tsr_simulate(policy_x, policy_o, n_games = 100L, seed = NULL, verbose = TRUE)
}
\arguments{
\item{policy_x}{Policy for player X. See \code{tsr_play_game()}.}

\item{policy_o}{Policy for player O.}

\item{n_games}{Integer. Number of games to play (default \code{100L}).}

\item{seed}{Optional integer. Seeds the RNG via \code{withr::local_seed()};
global \code{.Random.seed} is unchanged.}

\item{verbose}{Logical. Show a progress bar in interactive sessions.}
}
\value{
A tibble, one row per game, with columns:
\code{game_id} (integer), \code{policy_x} (character), \code{policy_o} (character),
\code{winner} (integer \code{0/1/2}), \code{n_moves} (integer),
\code{first_move} (integer cell),
\code{first_move_i}, \code{first_move_j}, \code{first_move_k}, \code{first_move_l} (integer).
}
\description{
Runs \code{n_games} self-play games and returns a tibble summarising each game.
}
\details{
Performance: pure-R self-play over 256 cells is slow, and deep-AI policies
multiply the cost. Meaningful opening statistics may need thousands of
games; the hot-path functions are flagged for future Rcpp replacement.
}
\examples{
\donttest{
tsr_simulate("random", "random", n_games = 5L, seed = 1L, verbose = FALSE)
}
}
