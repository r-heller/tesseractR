% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsr_simulate.R
\name{tsr_play_game}
\alias{tsr_play_game}
\title{Play a single game between two policies}
\usage{
tsr_play_game(policy_x, policy_o, seed = NULL)
}
\arguments{
\item{policy_x}{Policy for player X. Either a built-in string or a function
\verb{(board) -> integer cell index}.}

\item{policy_o}{Policy for player O. Same forms as \code{policy_x}.}

\item{seed}{Optional integer. If supplied, RNG is seeded via
\code{withr::local_seed()}; global \code{.Random.seed} is unchanged.}
}
\value{
A \code{tsr_game} object: list with \code{moves} (integer vector of cell
indices, in order), \code{winner} (\code{0/1/2}), \code{n_moves} (integer),
\code{policies} (named character \code{c(x, o)}), \code{final_board} (a \code{ttt_board}),
\code{to_move} (integer vector — moving player at each ply).
}
\description{
Simulates one complete 4D tic-tac-toe game with the given policies for
player X and player O. Returns a \code{tsr_game} object recording the move
sequence and outcome.
}
\details{
Built-in policy strings: \code{"random"}, \code{"greedy"}, \code{"ai"} (depth 2), or
\code{"aiN"} for \code{N} in \code{1:4} (e.g. \code{"ai3"}).
}
\examples{
g <- tsr_play_game("random", "random", seed = 1L)
g$winner
}
