% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsr_analyze.R
\name{tsr_analyze_game}
\alias{tsr_analyze_game}
\title{Analyze a played game move by move}
\usage{
tsr_analyze_game(game, method = c("heuristic", "rollout"), n = 200L)
}
\arguments{
\item{game}{A \code{tsr_game} object.}

\item{method}{One of \code{"heuristic"} (default) or \code{"rollout"}. Passed to
the underlying \code{tsr_win_prob()} / \code{tsr_rate_moves()}.}

\item{n}{Integer. Rollouts when \code{method = "rollout"}.}
}
\value{
A tibble, one row per ply, with columns:
\code{ply}, \code{player}, \code{cell}, \code{i}, \code{j}, \code{k}, \code{l},
\code{win_prob_before}, \code{win_prob_after}, \code{delta},
\code{best_cell}, \code{best_delta}, \code{regret},
\code{is_best}, \code{missed_win}, \code{missed_block}, \code{is_turning_point}.
}
\description{
Walks a \code{tsr_game} (from \code{tsr_play_game()}) and computes per-ply evaluation
context using the S3 evaluation core: win probability before/after each move,
the move's delta, the best alternative move, regret, and flags for
missed wins / missed blocks / turning points.
}
\details{
A turning point is a ply with \code{abs(delta) >= 0.15}.
}
\examples{
\donttest{
g <- tsr_play_game("random", "random", seed = 1L)
tsr_analyze_game(g)
}
}
